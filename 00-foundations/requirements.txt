# Runtime pinned to the versions these results were produced with.
# Install into a project-local environment:
#   python -m venv .venv && .venv/Scripts/activate   (Windows)
#   python -m venv .venv && source .venv/bin/activate (macOS/Linux)
#   pip install -r requirements.txt

numpy>=1.25,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<0.14

# Notebooks
jupyter>=1.0
notebook>=7.0

# Tests
pytest>=8.0
